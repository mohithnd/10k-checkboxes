# docker-compose.yml
services:
  redis:
    image: redis:7
    container_name: checkbox-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data

volumes:
  redis-data:
